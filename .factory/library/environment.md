# Environment

**External Dependencies:**
- PostgreSQL database
- chuumai-tools Docker containers (`ghcr.io/leomotors/maimai-scraper:v1` and `ghcr.io/leomotors/chunithm-scraper:v6`) outputting JSON files.

**Environment Variables:**
- `DATABASE_URL`: Connection string for PostgreSQL (local or Neon).
